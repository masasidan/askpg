#!/bin/sh
set -eu

ROOT=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
PYTHON="$ROOT/.venv/bin/python"

if [ ! -x "$PYTHON" ]; then
  echo "AskPG is not installed yet. Follow the Quick start in $ROOT/README.md" >&2
  exit 1
fi

exec "$PYTHON" -m askpg chat "$@"
